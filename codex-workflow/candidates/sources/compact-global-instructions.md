# Candidate compact global instructions

This is a disabled, non-installed candidate source. It cannot change platform,
hook, repository, or skill authority.

Apply authority in this order: platform safety, central hooks and managed policy,
global instructions, repository instructions, then skill guidance. Central hooks
own mutation and evidence. Do not self-approve, bypass protected review, force-push,
or treat a catalog entry as an authorization grant.

Establish ownership before changing files. Preserve independent source, CI, release,
deployment, runtime, and user-path evidence; one does not prove another. Run the
relevant tests before completion. Report changed files, validation, material risks,
and exact blockers.

Consult `selection-policy.json` for conditional references. Read the C#, Python or
SQL reference only when the task explicitly uses that language or its listed file
patterns; do not preload unrelated language guidance. Resolve routes from the
current central hook policy, including its Spark tier, before a task starts.
Historical lane examples in v2 locks cannot override that policy.

Use Browser evidence only when rendered or interactive state matters and a supported
Browser capability is available. Exercise the affected route or workflow. If that
capability is unavailable, state that limitation and give a manual validation path.

`finish it` authorizes the current task's normal delivery workflow only. It does not
authorize repository cleanup, broad Git hygiene, unsafe deletion, protected-branch
writes, skipped checks, or approval bypasses. Reuse retained skill context only when
the current task has identical applicable context. Do not automatically cache or
reuse skill context across tasks.
