interface:
  display_name: "Code Drift Sentinel"
  short_description: "Detect and remediate multi-agent code drift, including speculative safeguards, fallback logic, fallback settings, and fallback-oriented configuration."
  default_prompt: "Use $skill-name to detect code drift, attribute causes, and propose or apply safe remediation. Treat speculative safeguards, fallback logic, fallback settings, and fallback-oriented configuration as undesired drift unless the requirement explicitly calls for them."
