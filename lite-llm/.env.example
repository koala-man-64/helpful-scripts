# Copy to .env and substitute existing values. Never commit the populated file.
# Compose reads this as raw KEY=value: do not surround values with quotes.
LITELLM_MASTER_KEY=<LITELLM_KEY>
AZURE_AI_API_KEY=replace-with-your-existing-Foundry-key
